#include<stdio.h>
int main()
{
    int k,n,w;
    scanf("%d %d %d",&k,&n,&w);
    int sum=0;
    for (int i = 1; i <= w; i++)
    {
        sum=sum+(k*i);
    }
    if (sum==n || sum<n)
    {
        printf("0");
    }
    else if(sum>n)
    {
    printf("%d",sum-n);
    }
    return 0;
}
